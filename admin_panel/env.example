######################
# Admin Panel config #
######################
PUBLIC_SPEKTRUM_SERVER_URL=https://spektrum.example.com
PUBLIC_SPEKTRUM_WS_SERVER_URL=wss://spektrum.example.com/ws

# CDN URL for serving media files (leave empty for server-served media)
PUBLIC_SPEKTRUM_CDN_URL=https://cdn.example.com

# Development admin password (optional, dev mode only)
# Set this to auto-login in dev mode and skip the auth form
# Leave empty to use the normal login form
# Don't set this in production.
PUBLIC_DEV_ADMIN_PASSWORD=dummypassword123
